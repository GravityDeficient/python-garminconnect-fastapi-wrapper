fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# garth dropped in 0.3.x; uses curl_cffi for Chrome TLS fingerprinting.
# 0.3.4 not yet on PyPI as of 2026-05-09 — install from git master.
garminconnect @ git+https://github.com/cyberjunky/python-garminconnect@master
curl_cffi>=0.6
ua-generator>=1.0
python-dotenv>=1.0.0
prometheus_client>=0.19.0
